import 'package:flutter/material.dart';

class ColorUtils {
//static var primaryColor = Colors.red[900];
  static var primaryColor = Colors.teal;
  static var accentColor = Colors.amber;
  static var secondaryColor = Colors.deepOrange;
}
